package redis.clients.jedis;


public class SentinelFactory {
	public static JedisKeyedPool pool;

	// public static void SentinelFactory(Config poolConfig, Sentinel sentinel,
	// String password) {
	// pool = new JedisKeyedPool(poolConfig, sentinel, password);
	// }

}
